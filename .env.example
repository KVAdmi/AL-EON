# ============================================
# AL-EON - CONSOLA DE CHAT CON AL-E CORE
# ============================================
# Copia este archivo como .env y configura tus valores

# AL-E Core API (ÚNICO ENDPOINT - POST ONLY)
# NO uses OpenAI keys aquí - viven en AL-E Core
VITE_ALE_CORE_URL=https://api.al-entity.com/api/ai/chat

# Workspace y configuración
VITE_WORKSPACE_ID=al-eon
VITE_DEFAULT_MODE=universal

# Usuario (fundadores: patty, luis)
VITE_USER_ID=patty

# 🔧 FEATURE FLAGS
# Modo voz (micrófono + TTS)
# ⚠️ DESACTIVADO por default por problemas de estabilidad
# Cambiar a 'true' solo cuando esté corregido el error de inicialización
VITE_VOICE_MODE_ENABLED=false

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# - Este frontend NO necesita OpenAI keys
# - Solo se comunica con AL-E Core (EC2)
# - AL-E Core maneja la memoria, sesiones y emociones
# - El endpoint es POST-only (no GET)
# - Respuestas vienen en: answer, displayText.answer, o message
